apiVersion: agentspec.orchestrator.dev/v2alpha1
kind: Environment
metadata:
  key: dev
spec:
  name: Development
  isDefault: true
  networkPolicy:
    egressMode: open
  secrets:
    requiredKeys: []
  toolPolicy:
    preset: workspace-write
    rules: []
