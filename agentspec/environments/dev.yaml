apiVersion: agentspec.orchestrator.dev/v2alpha1
kind: Environment
metadata:
  key: dev
spec:
  name: Development
  isDefault: true
  networkPolicy:
    egressMode: open
  secrets:
    requiredKeys:
      - OPENAI_API_KEY
      - GITHUB_TOKEN
  toolPolicy:
    preset: workspace-write
    rules: []
